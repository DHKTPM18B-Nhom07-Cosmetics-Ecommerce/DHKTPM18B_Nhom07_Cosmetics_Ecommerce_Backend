package iuh.fit.se.cosmeticsecommercebackend.model.enums;
/**
 * phạm vi giảm giá, toàn sàn, brand, category
 */
public enum VoucherScope {
    GLOBAL,    // Giảm toàn sàn
    BRAND,     // Giảm theo thương hiệu
    CATEGORY   // Giảm theo danh mục
}
